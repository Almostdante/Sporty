[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --html=reports/report.html
    --self-contained-html
markers =
    ui: UI/E2E tests requiring a browser (Selenium)
    api: API-level tests using requests
